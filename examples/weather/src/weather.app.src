{application, weather, [
    {description, "Weather MCP server (stdio)"},
    {vsn, "0.1.0"},
    {registered, []},
    {mod, {weather_app, []}},
    {applications, [kernel, stdlib, erlmcp]},
    {start_phases, [{serve, []}]},
    {env, []}
]}.
